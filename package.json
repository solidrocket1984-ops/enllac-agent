{
  "name": "enllac-agent",
  "version": "2.0.0",
  "description": "Production-ready backend for DemoEnllacAgent",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "NODE_ENV=development node server.js",
    "start": "node server.js",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "lint": "node --check server.js && node --check src/server.js && node --check src/app.js"
  },
  "keywords": [
    "express",
    "agent",
    "openai"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
